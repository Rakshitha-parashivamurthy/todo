import { getCompaniesByAdmin, updateCompanyStatus, getPendingCompanies, createCompany, getCompanyById } from './repos/firestoreCompanies';

// Re-export methods for convenience or add any wrappers if necessary
export {
  getCompaniesByAdmin,
  updateCompanyStatus,
  getPendingCompanies,
  createCompany,
  getCompanyById
};
